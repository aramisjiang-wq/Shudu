# Server Configuration
SERVER_PORT=8080
NODE_ENV=development

# JWT Secret (生产环境请使用强密码)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Client Origin (前端地址)
CLIENT_ORIGIN=http://localhost:5173

# Vercel 部署时会自动设置以下变量：
# VERCEL_URL - Vercel 自动提供的域名
# NODE_ENV=production

